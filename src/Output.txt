calss AppTest(TestCase): 
	fixtures = ['auth']
	def setUp(self):
		self.client = Client()
		self.assertTrue(self.client.login(username='admin',password='admin'))
	def testMail_Compose:
		response = self.client.get('mail/compose')
		self.assertEqual(response.status_code,200)
		self.assertTemplateUsed(response,'')
	def testMail_Search:
		response = self.client.get('mail/search')
		self.assertEqual(response.status_code,200)
		self.assertTemplateUsed(response,'')
	def testMail_Home:
		response = self.client.get('mail/home')
		self.assertEqual(response.status_code,200)
		self.assertTemplateUsed(response,'')
	def testMail_Inbox_Archive:
		response = self.client.get('mail/inbox_archive')
		self.assertEqual(response.status_code,200)
		self.assertTemplateUsed(response,'')
	def testMail_See_Label:
		response = self.client.get('mail/see_label')
		self.assertEqual(response.status_code,200)
		self.assertTemplateUsed(response,'')
	def testMail_See_Label_Archive:
		response = self.client.get('mail/see_label_archive')
		self.assertEqual(response.status_code,200)
		self.assertTemplateUsed(response,'')
	def testMail_See_Thread:
		response = self.client.get('mail/see_thread')
		self.assertEqual(response.status_code,200)
		self.assertTemplateUsed(response,'')
	def testMail_See_Thread_Direct:
		response = self.client.get('mail/see_thread_direct')
		self.assertEqual(response.status_code,200)
		self.assertTemplateUsed(response,'')
	def testMail_Thread_Mark:
		response = self.client.get('mail/thread/mark')
		self.assertEqual(response.status_code,200)
		self.assertTemplateUsed(response,'')
	def testNone:
		response = self.client.get('None')
		self.assertEqual(response.status_code,200)
		self.assertTemplateUsed(response,'')
